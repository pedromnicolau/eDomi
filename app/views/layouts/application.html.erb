<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "eDomi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/favicon.png" type="image/png">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
      :root {
        --primary: #1A2E66;
        --secondary: #4ADE80;
        --white: #fff;
        --black: #000;
        --muted: #6c757d;
        --card-bg: #ffffff;
        --page-bg: #f7f9fc;
      }

      body {
        background: var(--white);
        color: var(--black);
      }

      .navbar {
        background-color: var(--primary) !important;
      }

      .navbar-brand,
      .nav-link,
      .navbar .btn {
        color: var(--white) !important;
      }

      .nav-link:hover,
      .navbar-brand:hover {
        color: var(--secondary) !important;
      }

      .form-label {
        color: var(--primary);
      }

      .form-control:focus {
        border-color: var(--secondary);
        box-shadow: 0 0 0 0.2rem rgba(74, 222, 128, 0.25);
      }

      .btn-primary {
        background-color: var(--primary) !important;
        border-color: var(--primary) !important;
      }

      .btn-secondary {
        background-color: var(--secondary) !important;
        color: var(--black) !important;
      }

      .profile-icon {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 56px;
        height: 56px;
        border-radius: 50%;
        font-size: 2rem;
        color: var(--white);
        background: transparent;
        cursor: pointer;
      }

      a {
        text-decoration: none;
      }

      /* Footer styles */
      .site-footer {
        background-color: var(--primary);
        color: var(--white);
        padding: 2rem 0;
      }
      .site-footer h5 {
        color: var(--white);
        margin-bottom: 0.75rem;
      }
      .site-footer p,
      .site-footer small {
        color: rgba(255,255,255,0.9);
      }
      .site-footer a {
        color: var(--white);
        text-decoration: none;
      }

      .page-section {
        background: linear-gradient(180deg, rgba(26,46,102,0.03) 0%, rgba(250,250,252,0.6) 100%);
        padding-top: 2.5rem;
        padding-bottom: 2.5rem;
      }

      .page-hero {
        text-align: left;
        margin-bottom: 1rem;
      }
      .page-hero h1 {
        font-size: 2rem;
        font-weight: 700;
        color: var(--primary);
        margin-bottom: 0.25rem;
      }
      .page-hero .lead {
        color: var(--muted);
        margin-bottom: 0;
      }

      .page-card {
        background: var(--card-bg);
        border-radius: 0.5rem;
        padding: 1.5rem;
        box-shadow: 0 8px 20px rgba(11, 22, 50, 0.06);
      }

      .page-card h3 {
        color: var(--primary);
        margin-top: 1rem;
      }

      .page-card p {
        color: #334155;
        line-height: 1.6;
      }

      /* Contact specifics */
      .contact-info p a {
        color: var(--primary);
        text-decoration: underline;
      }

      .contact-form .form-control {
        border-radius: 0.375rem;
      }

      .contact-form .btn-primary {
        border-radius: 0.375rem;
        padding: 0.5rem 1rem;
      }

      /* Responsive tweaks */
      @media (min-width: 768px) {
        .page-hero h1 { font-size: 2.25rem; }
      }

      .profile-icon.dropdown-toggle::after {
        font-size: 1.2rem; /* tamanho da setinha */
        margin-left: 4px; /* espaço entre o ícone e a seta */
        vertical-align: middle;
      }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <img src="/logo_ed.png" alt="eDomi" style="height: 60px; width: auto; margin-right: 10px;">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
          <!-- Links centrais -->
          <ul class="navbar-nav mb-2 mb-lg-0">
            <li class="nav-item mx-2">
              <%= link_to "Imóveis", properties_path, class: "nav-link" %>
            </li>
            <li class="nav-item mx-2">
              <%= link_to "Sobre", about_path, class: "nav-link" %>
            </li>
            <li class="nav-item mx-2">
              <%= link_to "Contato", contact_path, class: "nav-link" %>
            </li>
          </ul>
        </div>

        <!-- Dropdown de perfil / login -->
        <div class="d-flex align-items-center">
          <% if user_signed_in? %>
            <div class="dropdown">
              <a class="profile-icon dropdown-toggle" href="#" role="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fa-solid fa-circle-user"></i>
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown">
                <li>
                  <%= link_to "Perfil", edit_user_registration_path, class: "dropdown-item" %>
                </li>
                <li>
                  <%= link_to "Sair", destroy_user_session_path,
                              data: { "turbo-method": :delete, turbo_confirm: "Tem certeza que deseja sair?" },
                              class: "dropdown-item" %>
                </li>
              </ul>
            </div>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "nav-link fw-semibold ms-3" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container my-4">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="site-footer mt-5">
      <div class="container">
        <div class="row">
          <div class="col-md-4 mb-3">
            <h5>eDomi</h5>
            <p>Encontre seu novo lar com praticidade e confiança. Listagens verificadas e atendimento especializado.</p>
          </div>

          <div class="col-md-4 mb-3">
            <h5>Contato</h5>
            <p>
              Email: <a href="mailto:contato@edomi.com.br">contato@edomi.com.br</a><br>
              Telefone: <a href="tel:+551112345678">(11) 1234-5678</a>
            </p>
          </div>

          <div class="col-md-4 mb-3">
            <h5>Endereço</h5>
            <p>Rua Exemplo, 123 - Centro<br>São Paulo - SP</p>
          </div>
        </div>

        <hr style="border-color: rgba(255,255,255,0.1);" />

        <div class="d-flex flex-column flex-md-row justify-content-between align-items-center">
          <small class="mb-2 mb-md-0">&copy; <%= Time.current.year %> eDomi. Todos os direitos reservados.</small>
          <div>
            <%= link_to "Política de Privacidade", privacy_policy_path, class: "me-3" %>
            <%= link_to "Termos de Uso", terms_path, class: "" %>
          </div>
        </div>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
