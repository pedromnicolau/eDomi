<div class="container py-4 page-section">
  <div class="page-hero">
    <h1>Entrar</h1>
    <p class="lead">Acesse sua conta para gerenciar suas propriedades e favoritos.</p>
  </div>

  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="page-card">
        <%# mostra erros do Devise se houver %>
        <%= devise_error_messages! %>

        <%= form_with scope: resource_name, url: session_path(resource_name), local: true do |f| %>
          <div class="mb-3">
            <%= f.label :email, "Email", class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", value: (resource&.email || "") %>
          </div>

          <div class="mb-3">
            <%= f.label :password, "Senha", class: "form-label" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
          </div>

          <div class="mb-3 form-check">
            <%= f.check_box :remember_me, class: "form-check-input", id: "remember_me" %>
            <%= f.label :remember_me, "Lembrar-me", class: "form-check-label", for: "remember_me" %>
          </div>

          <div class="d-flex justify-content-between align-items-center">
            <%= f.submit "Entrar", class: "btn btn-primary" %>
            <div>
              <%= link_to "Esqueceu a senha?", new_password_path(resource_name), class: "text-muted ms-3" %>
            </div>
          </div>
        <% end %>

        <hr class="my-4">

        <div class="text-center">
          <p class="mb-1">Ainda não tem conta?</p>
          <%= link_to "Criar conta", new_registration_path(resource_name), class: "btn btn-outline-secondary" %>
        </div>
      </div>
    </div>
  </div>
</div>
