Bem-vindo(a) <%= @resource.name || @resource.email %>!

Sua conta no eDomi foi criada com sucesso.

Para confirmar sua conta, acesse o link abaixo:

<%= confirmation_url(@resource, confirmation_token: @token) %>

Atenciosamente,
Equipe eDomi
