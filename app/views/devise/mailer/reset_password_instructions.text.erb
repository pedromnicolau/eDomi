Olá <%= @resource.name || @resource.email %>!

Recebemos uma solicitação para redefinir a senha da sua conta no eDomi.

Para criar uma nova senha, acesse o link abaixo:

<%= "#{root_url}users/password/edit?reset_password_token=#{@token}" %>

Se você não solicitou esta alteração, por favor ignore este e-mail.

Sua senha não será alterada até que você acesse o link acima e crie uma nova senha.

Atenciosamente,
Equipe eDomi
